var mainContentScroll = function () {
  $('main').animate({scrollTop: $('#all-pages').offset().top - 63}, 1000)
}

$(function () {
  $(document).on('click', '#down-arrow-button', mainContentScroll);
})
